/* tipos de datos */
/* strings cadenas de caracteres */
const nombre = 'Julian'

// numeros
const edad = 27

// booleanos
const esAdulto = true // false

// indefinido undefined
let usuario

// null
const datosExtra = null

console.log(datosExtra)
console.log(usuario)

// Objetos Javascript / Objetos JSON (JavaScript Object Notation)
const persona = {
    nombre: 'Julian',
    edad: 27,
    esAdulto: true,
    datosExtra: null
}

// Arreglos
const miArreglo = ['julian', 27, true, null, 23.64, {nombre: 'julian'}, ['pera', 'manzana']]

